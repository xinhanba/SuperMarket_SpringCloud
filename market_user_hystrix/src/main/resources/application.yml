server:
  port: 8078

eureka:
  client:
    service-url:
      defaultZone: http://peer1:8761/eureka/,http://peer2:8762/eureka/,http://peer3:8763/eureka/

spring:
  application:
    name: eureka_hystrix_user
## 如果不添加此信息，fallbackfactory无法打印日志信息
feign:
  hystrix:
    enabled: true